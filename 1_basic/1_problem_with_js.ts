function add(num1: number, num2: number): number {
  return num1 + num2;
}

console.log(add(1, 2)); // 3
// console.log(add(1, '2')); // 컴파일 에러 발생

/**
 * TS를 사용하면 변수/함수/클래스 정의 등 타입을 명확하게 하여
 * 작성된 코드가 원래 의도한 대로 사람들이 사용할 수 있도록 강제할 수 있다.
 */